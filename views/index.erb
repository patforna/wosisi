<div class="container">
  <header>
    <h1>Wo si si?</h1>
  	<section id="last-tweet">
  			<p class="tweet-text"><%= auto_link(@last_tweet.text) %></p>
  			<p class="tweet-age"><%= ago_in_words(@last_tweet.created_at) %></p>
  			<p class="tweet-location">TBD</p>
  			<details>
  			  <span class="latitude"><%= @last_place.latitude %></span>, <span class="longitude"><%= @last_place.longitude %></span>
  			</details>
  	</section>	
	</header>
	<section id="map_canvas"></section>
</div>
